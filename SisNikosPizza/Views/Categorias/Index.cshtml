@model IEnumerable<SisNikosPizza.Domain.Models.Categoria>
@{
    ViewData["Title"] = "Lista de Categorías";
    Layout = "~/Views/Shared/_Admin.cshtml";
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-12 col-lg-8">
            <div class="card">
                <div class="card-header">
                    <div class="d-flex justify-content-between">
                        <h2>Categorías</h2>
                        <div>
                            <a class="btn btn-success btn-sm mt-2" asp-controller="Index" asp-action="Create">
                                <i class="bi bi-file-earmark-plus"></i>&nbsp;Nueva Categoría
                            </a>
                        </div>
                    </div>
                </div>

                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-sm" id="table_categories">
                            <thead class="thead">
                                <tr>       
                                    <th class="text-center">Opciones</th>
                                    <th class="text-center">CategoroiaId</th>
                                    <th class = "text_center">nombre</th>
                                    <th class="text-center">Estado</th>                                    
                                </tr>
                            </thead>
                            <tbody>
                                @* @foreach (var item in Model){
                                    <tr>
                                        <td>@item.CategoryId</td>
                                        <td>@item.Name</td>
                                        <td>
                                            @if(item.Status){
                                                <span class="text-success">ACTIVO</span>
                                            }else{
                                               <span class="text-danger">INACTIVO</span>
                                            }
                                         </td>
                                        <td>
                                            <a asp-controller="Categories" asp-action="Edit" asp-route-id="@item.CategoryId" class="btn btn-warning btn-sm">
                                                Editar 
                                            </a>
                                            <a class="btn btn-info btn-sm">
                                                Ver
                                            </a>
                                            <a asp-controller="Categories" asp-action="Delete" asp-route-id="@item.CategoryId" class="btn btn-danger btn-sm">
                                                Eliminar
                                            </a>
                                        </td>
                                    </tr>
                                } *@
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    @* <script>
        $(document).ready(function () {
            $('#table_categories').DataTable();
        });
    </script> *@

    <script src="~/js/categoria.js"></script>
}